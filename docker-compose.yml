services:
 postgres:
   container_name: desafio_vagas_api
   image: postgres
   ports:
    - 5432:5432
   environment:
    - POSTGRES_USER=admin
    - POSTGRES_PASSWORD=admin
    - POSTGRES_DB=desafio_vagas_api